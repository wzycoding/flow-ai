server {
    listen 3000;
    server_name localhost;

    resolver 127.0.0.11 valid=10s ipv6=off;
    set $flow_ai_api http://flow-ai-api:5001;

    # Docker Compose中直接访问UI端口时，将API请求代理到后端服务。
    location /api/ {
        rewrite ^/api/(.*)$ /$1 break;
        proxy_pass $flow_ai_api;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_buffering off;
        proxy_read_timeout 600s;
        proxy_send_timeout 600s;
    }

    # 如果有其他文件（图片、字体、js脚本、样式表等），使用Nginx静态提供
    location ~* \.(?:css|js|jpe?g|gif|svg|woff|woff2|eot|ttf|otf|ico|webp)$ {
        root /usr/share/nginx/html;
        expires 1y;
        add_header Cache-Control "public, no-transform";
    }

    # 访问静态页面时将路径转发到/index.html上，适用于单页面应用
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }
}
